#pragma once

#include <QWidget>
#include <QStyledItemDelegate>

class VectorItemDelegate : public QStyledItemDelegate
{
    Q_OBJECT
public:
    explicit VectorItemDelegate(QWidget * parent=nullptr);

public slots:



signals:

private:


};

